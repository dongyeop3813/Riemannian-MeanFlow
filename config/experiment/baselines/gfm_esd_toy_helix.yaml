# @package _global_

defaults:
  - /task/toy_helix
  - override /time_sampler: uniform_interval
  - _self_

trainer:
  _target_: src.experiments.GFM_ESD_Trainer

wandb:
  tags: ["GFM_ESD", "toy_helix"]