# @package _global_

defaults:
  - /task/promoter_dna
  - override /time_sampler: lognorm
  - _self_

trainer:
  _target_: src.experiments.FlowMatchingTrainer

loss_p: 0.0

wandb:
  tags: ["FisherFM", "promoter"]